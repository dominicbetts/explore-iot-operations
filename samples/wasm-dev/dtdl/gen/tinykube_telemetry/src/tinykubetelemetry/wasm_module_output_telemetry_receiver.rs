/* Code generated by Azure.Iot.Operations.ProtocolCompiler v0.9.0.0; DO NOT EDIT. */

use std::collections::HashMap;

use azure_iot_operations_mqtt::interface::{AckToken, ManagedClient};
use azure_iot_operations_protocol::application::ApplicationContext;
use azure_iot_operations_protocol::common::aio_protocol_error::AIOProtocolError;
use azure_iot_operations_protocol::telemetry::telemetry_receiver::{
    TelemetryMessage as TelemetryMessageBase, TelemetryReceiver as TelemetryReceiverBase,
    TelemetryReceiverOptionsBuilder as TelemetryReceiverOptionsBaseBuilder,
};

use super::super::common_types::common_options::TelemetryOptions;
use super::wasm_module_output_telemetry::WasmModuleOutputTelemetry;
use super::MODEL_ID;
use super::TELEMETRY_TOPIC_PATTERN;

pub type WasmModuleOutputTelemetryMessage = TelemetryMessageBase<WasmModuleOutputTelemetry>;

/// Telemetry Receiver for `WasmModuleOutputTelemetry`
pub struct WasmModuleOutputTelemetryReceiver<C>(
    TelemetryReceiverBase<WasmModuleOutputTelemetry, C>,
)
where
    C: ManagedClient + Clone + Send + Sync + 'static,
    C::PubReceiver: Send + Sync + 'static;

impl<C> WasmModuleOutputTelemetryReceiver<C>
where
    C: ManagedClient + Clone + Send + Sync + 'static,
    C::PubReceiver: Send + Sync + 'static,
{
    /// Creates a new [`WasmModuleOutputTelemetryReceiver`]
    ///
    /// # Panics
    /// If the DTDL that generated this code was invalid
    pub fn new(
        application_context: ApplicationContext,
        client: C,
        options: &TelemetryOptions,
    ) -> Self {
        let mut receiver_options_builder = TelemetryReceiverOptionsBaseBuilder::default();
        if let Some(topic_namespace) = &options.topic_namespace {
            receiver_options_builder.topic_namespace(topic_namespace.clone());
        }

        let mut topic_token_map: HashMap<String, String> = options
            .topic_token_map
            .clone()
            .into_iter()
            .map(|(k, v)| (format!("ex:{k}"), v))
            .collect();

        topic_token_map.insert("modelId".to_string(), MODEL_ID.to_string());
        topic_token_map.insert("telemetryName".to_string(), "wasmModuleOutput".to_string());

        let receiver_options = receiver_options_builder
            .topic_pattern(TELEMETRY_TOPIC_PATTERN)
            .topic_token_map(topic_token_map)
            .auto_ack(options.auto_ack)
            .build()
            .expect("DTDL schema generated invalid arguments");

        Self(
            TelemetryReceiverBase::new(application_context, client, receiver_options)
                .expect("DTDL schema generated invalid arguments"),
        )
    }

    /// Shut down the [`WasmModuleOutputTelemetryReceiver`]
    ///
    /// # Errors
    /// [`AIOProtocolError`] if there is a failure in graceful shutdown
    pub async fn shutdown(&mut self) -> Result<(), AIOProtocolError> {
        self.0.shutdown().await
    }

    /// Receive the next [`WasmModuleOutputTelemetryMessage`]
    ///
    /// # Errors
    /// [`AIOProtocolError`] if there is a failure receiving a message
    pub async fn recv(
        &mut self,
    ) -> Option<Result<(WasmModuleOutputTelemetryMessage, Option<AckToken>), AIOProtocolError>>
    {
        self.0.recv().await
    }
}
