/* Code generated by Azure.Iot.Operations.ProtocolCompiler v0.9.0.0; DO NOT EDIT. */

use std::collections::HashMap;
use std::time::Duration;

use azure_iot_operations_mqtt::control_packet::QoS;
use azure_iot_operations_mqtt::interface::ManagedClient;
use azure_iot_operations_protocol::application::ApplicationContext;
use azure_iot_operations_protocol::common::aio_protocol_error::AIOProtocolError;
use azure_iot_operations_protocol::common::payload_serialize::PayloadSerialize;
use azure_iot_operations_protocol::telemetry::telemetry_sender::{
    CloudEvent, TelemetryMessage as TelemetryMessageBase,
    TelemetryMessageBuilder as TelemetryMessageBuilderBase,
    TelemetryMessageBuilderError as TelemetryMessageBuilderBaseError,
    TelemetrySender as TelemetrySenderBase,
    TelemetrySenderOptionsBuilder as TelemetrySenderOptionsBaseBuilder,
};

use super::super::common_types::common_options::TelemetryOptions;
use super::wasm_module_output_telemetry::WasmModuleOutputTelemetry;
use super::MODEL_ID;
use super::TELEMETRY_TOPIC_PATTERN;

pub type WasmModuleOutputTelemetryMessage = TelemetryMessageBase<WasmModuleOutputTelemetry>;
pub type WasmModuleOutputTelemetryMessageBuilderError = TelemetryMessageBuilderBaseError;

/// Builder for [`WasmModuleOutputTelemetryMessage`]
#[derive(Default)]
pub struct WasmModuleOutputTelemetryMessageBuilder {
    inner_builder: TelemetryMessageBuilderBase<WasmModuleOutputTelemetry>,
}

impl WasmModuleOutputTelemetryMessageBuilder {
    /// Quality of Service of the telemetry message. Can only be `AtMostOnce` or `AtLeastOnce`.
    pub fn qos(&mut self, qos: QoS) -> &mut Self {
        self.inner_builder.qos(qos);
        self
    }

    /// Custom user data to set on the message
    pub fn custom_user_data(&mut self, custom_user_data: Vec<(String, String)>) -> &mut Self {
        self.inner_builder.custom_user_data(custom_user_data);
        self
    }

    /// Time before message expires
    pub fn message_expiry(&mut self, message_expiry: Duration) -> &mut Self {
        self.inner_builder.message_expiry(message_expiry);
        self
    }

    /// Cloud event for the message
    pub fn cloud_event(&mut self, cloud_event: Option<CloudEvent>) -> &mut Self {
        self.inner_builder.cloud_event(cloud_event);
        self
    }

    /// Payload of the message
    ///
    /// # Errors
    /// If the payload cannot be serialized
    pub fn payload(
        &mut self,
        payload: WasmModuleOutputTelemetry,
    ) -> Result<&mut Self, AIOProtocolError> {
        self.inner_builder.payload(payload)?;
        Ok(self)
    }

    /// Builds a new `WasmModuleOutputTelemetryMessage`
    ///
    /// # Errors
    /// If a required field has not been initialized
    pub fn build(
        &mut self,
    ) -> Result<WasmModuleOutputTelemetryMessage, WasmModuleOutputTelemetryMessageBuilderError>
    {
        self.inner_builder.build()
    }
}

/// Telemetry Sender for `WasmModuleOutputTelemetry`
pub struct WasmModuleOutputTelemetrySender<C>(TelemetrySenderBase<WasmModuleOutputTelemetry, C>)
where
    C: ManagedClient + Send + Sync + 'static;

impl<C> WasmModuleOutputTelemetrySender<C>
where
    C: ManagedClient + Send + Sync + 'static,
{
    /// Creates a new [`WasmModuleOutputTelemetrySender`]
    ///
    /// # Panics
    /// If the DTDL that generated this code was invalid
    pub fn new(
        application_context: ApplicationContext,
        client: C,
        options: &TelemetryOptions,
    ) -> Self {
        let mut sender_options_builder = TelemetrySenderOptionsBaseBuilder::default();
        if let Some(topic_namespace) = &options.topic_namespace {
            sender_options_builder.topic_namespace(topic_namespace.clone());
        }

        let mut topic_token_map: HashMap<String, String> = options
            .topic_token_map
            .clone()
            .into_iter()
            .map(|(k, v)| (format!("ex:{k}"), v))
            .collect();

        topic_token_map.insert("modelId".to_string(), MODEL_ID.to_string());
        topic_token_map.insert("senderId".to_string(), client.client_id().to_string());

        topic_token_map.insert("telemetryName".to_string(), "wasmModuleOutput".to_string());

        let sender_options = sender_options_builder
            .topic_pattern(TELEMETRY_TOPIC_PATTERN)
            .topic_token_map(topic_token_map)
            .build()
            .expect("DTDL schema generated invalid arguments");

        Self(
            TelemetrySenderBase::new(application_context, client, sender_options)
                .expect("DTDL schema generated invalid arguments"),
        )
    }

    /// Sends a [`WasmModuleOutputTelemetryMessage`]
    ///
    /// # Error
    /// [`AIOProtocolError`] if there is a failure sending the message
    pub async fn send(
        &self,
        message: WasmModuleOutputTelemetryMessage,
    ) -> Result<(), AIOProtocolError> {
        self.0.send(message).await
    }
}
